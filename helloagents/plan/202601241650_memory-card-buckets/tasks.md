# 任务清单：Memory Card buckets 最小实装（rules-based）

- [ ] 更新 `structured_condense()`：填充 `facts/preferences/constraints/decisions`
- [ ] 控制输出体积：去重 + 截断上限，避免 summary 膨胀
- [ ] 更新/新增单测：覆盖 buckets 抽取（含中文指令）
- [ ] 本地验证：跑 `python -m unittest discover -s server/tests`
- [ ] 更新知识库：在 `helloagents/modules/industry_alignment.md` 标记任务完成与验证方式

